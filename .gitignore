# Secrets — API keys live here, NEVER commit them
.env
*.key

__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.DS_Store

# Run artifacts (regenerated by `python run.py`)
out/
*.jsonl
report.html
integrity_certificate.json
accuracy.json

# Never commit evidence images or VM appliances
*.ova
*.vmdk
*.raw
*.E01
*.mem
*.dd

# Canary tripwires are seeded fresh each run, never committed
glassbox_canary_*.txt
